pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                # sh 'mvn --version'
                sh 'javac.exe helloJava.java'
            }
        }
    }
}
